0. Cái này assume người tải đã tải về Python. Nếu không có python thì mò cách tải nhen.
   Có thể kiểm tra = bước 1.
0. Tải một số thứ (từ sau không cần tải nữa). Vào cmd, ghi:

pip install rich keyboard playsound==1.2.2

Nếu tải thành công, start from here:

1. Copy path (Ctrl+Shift+C) của file `corp_time.py`
2. Vào cmd, ghi:

py path/vừa/copy/

3. Ghi số phút thuyết trình, tên các presenters, random,... Bật tiếng để có tín hiệu chuyển người nói.

Vậy thui nhen. Fighting for Philong!